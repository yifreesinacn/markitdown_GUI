# AnyToMD 简要使用指南

## 1. 适用环境

- 操作系统：Windows
- 推荐方式：优先在已安装相关依赖的本机环境中运行
- 常见依赖：MarkItDown（0.1.7 起其运行依赖含 magika / onnxruntime）、pdf2image、pdfplumber、PyMuPDF、python-docx、python-pptx、html2text、BeautifulSoup、lxml、pywin32
- 可选增强引擎（2026-09-01 新增）：Firecrawl AnyDoc（Office 旧版/新版兜底，支持 .doc/.docx/.ppt/.pptx/.xls/.xlsx）、Firecrawl pdf-inspector（纯文本 PDF）

## 2. 启动方式

### 2.1 运行 Python 主程序

```bash
python .\AnyToMD_main.py
```

### 2.2 运行已打包 EXE

- 路径：`dist/AnyToMD_v0.1.7_20260902.exe`

## 3. 主界面功能

- 单文件转换：适合逐个检查效果、预览结果
- 批量转换：适合同类文件集中处理
- 设置：控制输出模式、图片策略、OCR、嵌入图片等选项
- 关于：显示项目说明

## 4. 推荐使用顺序

- 第一步：先用单文件转换验证一个真实样本
- 第二步：根据样本效果调整“文档输出模式”
- 第三步：确认图片输出策略与输出目录符合预期
- 第四步：样本通过后再做批量转换

## 5. 各类文档推荐思路

### 5.1 PDF

- 默认重点：图文混排、尽量保持文字与图片相对位置
- 当前建议：直接使用默认智能路径
- 说明：PDF 当前已采用专用图文混排逻辑，效果优先，后续不建议随意改动
- 增强：纯文本型 PDF 可启用 pdf-inspector 引擎（设置页「PDF引擎策略」），自动识别 text_based 类型并输出高质量 Markdown；含图/扫描型 PDF 自动回退现有图文混排路径

### 5.2 Word

- 默认重点：保留正文、图片、段落关系
- 当前状态：整体表现较稳定
- 建议：默认先试智能模式，再根据需要切换到其他输出模式

### 5.3 PPT

- 默认重点：保留页面级内容与图片
- 注意：部分版式能力依赖本机 Office 环境
- 提示：追求“像原文档的外观”（含背景图、图形与版式）请使用「保持版式」（整页转图）；旧版 .ppt 与 .pptx 均已支持该模式（需本机安装 PowerPoint，耗时较长）；智能/文字模式适合提取可检索文本
- 提示：智能（图文并存）模式在装有 Office 的电脑上会自动“Office 渲染 PDF → 按页图文混排”，图文按页排列、效果接近版式；无 Office 电脑自动回退默认引擎（内容仍完整，但图片顺序可能不理想）

### 5.4 HTML / HTM / MHTML

- 默认重点：优先保留正文与图片，并尽量保留原页面中的相对顺序
- 注意：MHTML 先解析为 HTML 再进入转换链路
- 注意：普通网页默认会优先提取主内容，但只有在主内容区域足够明确时才会聚焦，否则仍保留整体内容
- 注意：浏览器导出的书签 HTML 会走专用解析路径（HTMLParser 事件流 + DL 栈），完整保留文件夹层级、链接与备注；如解析异常会自动以内嵌 HTML 视图兜底
- 注意：如果选择“保持版式”，依赖浏览器无头打印能力
- 提示：文件选择框已包含旧版 Excel（.xls）与旧版 PPT（.ppt）类型，二者转换兜底依赖 AnyDoc 引擎

## 6. 关键设置说明

### 6.1 文档输出模式

- 智能转换：优先兼顾内容与图文结构
- 保持版式：更强调页面外观，但依赖环境更多
- 保留图像：更强调图片输出
- 保留文字：更强调文字内容提取
- 保留文字（含识别文字）：适合 OCR 场景

### 6.2 图片输出策略

- 仅原位：尽量只在原位置输出图片
- 仅补缺：已有图则不重复，无图时补图
- 全量附录：适合需要最大保留图片资源的情况

### 6.3 PDF 引擎策略（2026-09-01 新增）

- 自动（默认）：detect_pdf 智能分类，text_based → pdf-inspector；scanned/mixed → 回退现有图文混排
- 图文混排优先：直接走现有图文混排路径（等同未启用 pdf-inspector）
- pdf-inspector优先：不预判类型，直接尝试 pdf-inspector，失败回退图文混排
- 说明：本策略仅作用于「智能转换」模式，保持版式与扫描件 OCR 路径不受影响

## 7. 推荐搭配

- PDF：默认智能转换
- Word：默认智能转换
- PPT：默认智能转换，必要时切换保持版式
- HTML / HTM / MHTML：默认智能转换，布局要求极高时再尝试保持版式
- 浏览器书签 HTML：优先用默认智能转换，不建议先切换到纯文字模式

## 8. 常见问题

### 8.1 转换成功但效果一般

- 先确认文档类型是否适合当前模式
- 再确认图片输出策略是否与目标一致
- 最后使用单文件模式复核样本效果

### 8.2 保持版式失败

- Word / PPT：检查 Office 是否可用
- HTML / HTM / MHTML：检查 Edge 或 Chrome 是否可用
- OCR：检查 Tesseract 是否安装

### 8.3 批量转换结果不一致

- 说明同批文件可能来源不同、结构不同
- 建议先按文件类型分批
- 建议先用代表性样本验证，再批量执行

### 8.4 图片或旧版 Office 转换结果为空 / 不支持

- 纯图片若不含 EXIF 或可读元数据，MarkItDown 可能返回空输出；可先确认样本是否带 EXIF，含文字的图片再尝试 OCR 相关模式
- 旧版 .xls / .ppt 的兜底依赖 AnyDoc 引擎：若未安装，可先在设置页「检查功能完整性」查看状态，确认提示为“未安装/未支持”

## 9. 使用注意事项

- 不要直接用批量转换代替首个样本验证
- PDF 当前效果较好，建议将其视为稳定基线
- HTML / MHTML / HTM 仍应优先做真实样本验证，尤其是门户页、书签导出页、复杂分栏页
- 如果输出用于发布，建议人工复核最终 Markdown
